import { DesignImmersive } from "@/components/DesignImmersive";

export default function DesignPage() {
  return (
    <main className="relative h-[100dvh] w-full overflow-hidden bg-black">
      <DesignImmersive />
    </main>
  );
}
